import os
from dotenv import load_dotenv, find_dotenv

load_dotenv()

OUTPUTS_DIR = os.path.join(config["output_dir"], config["llm"])

print(f"out: {OUTPUTS_DIR}/responses.tsv")


include: "rules/qa.smk"


rule all:
    input:
        f"{OUTPUTS_DIR}/responses.tsv",
    output:
        f"{OUTPUTS_DIR}/all-done",
    shell:
        "touch {output}"
